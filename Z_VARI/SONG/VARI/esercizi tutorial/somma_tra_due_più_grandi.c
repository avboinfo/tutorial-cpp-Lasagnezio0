#include <stdio.h>

int main()
{
    int x, y, z, sum;
    printf("inserisci il valore di x: ");
    scanf("%d", &x);
    printf("inserisci il valore di y: ");
    scanf("%d", &y);
    printf("inserisci il valore di z: ");
    scanf("%d", &z);

    return 0;
}